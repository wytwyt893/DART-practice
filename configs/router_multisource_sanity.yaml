experiment_name: router_multisource_sanity
seed: 42

data:
  num_samples: 1200
  text_dim: 4096
  image_dim: 4096
  question_dim: 1920
  num_routes: 3
  train_ratio: 0.8

model:
  text_dim: 4096
  image_dim: 4096
  question_dim: 1920
  hidden_dim: 256
  dropout: 0.1
  num_routes: 3

training:
  epochs: 10
  train_batch_size: 64
  val_batch_size: 128
  lr: 0.001

outputs:
  log_file: logs/router_multisource_sanity_log.txt
  last_checkpoint: checkpoints/router_multisource_sanity_last.pth
  best_checkpoint: checkpoints/router_multisource_sanity_best.pth